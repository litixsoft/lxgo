version: '3'
services:
  lxgo:
    image: golang:1.11
    volumes:
      - ../:/root/app
    working_dir: /root/app
    environment:
      - DB_HOST=lxgo-mongodb
    links:
      - lxgo-mongodb
    command: >
      sh -c "go version
      && pwd
      && ls -la
      && go test -v -cover ./..."
  lxgo-mongodb:
    image: mongo:4.0
    command: mongod --smallfiles --logpath=/dev/null # --quiet